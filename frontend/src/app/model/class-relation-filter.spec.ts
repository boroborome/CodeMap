import { ClassRelationFilter } from './class-relation-filter';

describe('ClassRelationFilter', () => {
  it('should create an instance', () => {
    expect(new ClassRelationFilter()).toBeTruthy();
  });
});
